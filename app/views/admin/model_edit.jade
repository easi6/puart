extend ./layout.jade

block content
  .row
    .col-lg-12
      h1.page-header
        | Editing #{inflection.capitalize(model.name)}##{record.id || record.getDataValue(model.primaryKeyAttributes[0])}
      include ./breadcrumb.jade
  include ./form.jade
